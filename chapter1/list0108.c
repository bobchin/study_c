/*
 * 2 つの変数に整数値を格納して表示
 */
#include <stdio.h>

int main(void)
{
    int vx, vy;

    vx = 57;
    vy = vx + 10;

    printf("vx の値は %d です。\n", vx);
    printf("vy の値は %d です。\n", vy);

    return (0);
}
